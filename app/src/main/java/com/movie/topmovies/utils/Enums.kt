package com.movie.topmovies.utils

/**
 * For Network response
 */
enum class NetworkStatus {
    SUCCESS,
    ERROR,
    LOADING
}

/**
 * For Network response
 */
enum class PagedStatus {
    PAGE,
    ERROR,
    COMPLETED
}